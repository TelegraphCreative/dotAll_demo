{# Contact Item #}

{# {% if block.photo|length %}pt-8{% endif %}  #}
<div class="list-contacts__contact border-b border-grey-lightest pb-8  sm:flex">
	
	{# {% if block.photo|length %}
	<div class="contact__img sm:w-1/3 w-2/3 m-auto sm:border-r border-grey-lightest">
		<div class="max-w-xs m-auto sm:w-full mb-6 px-6 md:px-0 md:pr-8">
			<figure class="img is1by1">
				{{image.single_transform(block.photo[0],'square','rounded-full absolute pin-t pin-l')}}	
			</figure>
		</div>
	</div>
	{% endif %} #}
	
	<div class="contact__info   text-center sm:text-left sm:w-2/3 px-6 md:pl-8 mt-8 md:mt-12">
		
		<h4 class="text-blue text-1md sm:text-2md mb-2 md:mb-4">{{content.firstName}} {{content.lastName}}</h4>

{# 		{% if block.role|length %}
			<h6 class="text-blue-light text-base sm:text-md mb-2 md:mb-4">{{block.role}}</h6>
		{% endif %}
		

		<div class="text-base text-grey-dark m-auto w-full leading-normal">

			{% if block.phone != '(###) ###-####' %}
				<a href="tel:{{block.phone}}" class="block no-underline text-grey-dark hover:text-blue">{{block.phone}}</a>
			{% endif %}

			{% if block.email|length %}
				<a href="tel:{{block.phone}}" class="block no-underline text-grey-dark hover:text-blue">{{block.email}}</a>
			{% endif %}

			{% if block.other|length %}
				{{block.other}}
			{% endif %}
			
		</div> #}
	</div>

</div>